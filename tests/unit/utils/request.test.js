import request from '@/utils/request'
describe('[utils/common]', () => {
  beforeAll(() => {
  })
  afterAll(() => {
  })
  it('1.1 ', () => {
    const result = 1 + 1
    expect(result).toBe(2)
  })
})
